<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ header.name }} - CV</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      font-size: 1.0rem;
      margin: 0;
      padding: 0;
      line-height: 1.4;
      color: #515151;
      background: #fff;
    }

    a {
      color: #515151;
    }

    header {
      /* text-align: center; */
      display: flex;
      align-items: center;
      padding: 1rem 1rem;
      justify-content: space-between;
      /* background: #f5f5f5; */
      margin: auto;
      margin-bottom: 1rem;
      max-width: 1200px;
      border-bottom: 2px solid #ddd;
    }

    header .text-block {
      flex: 1;
      /* max-width: calc(100% - 110px); */ /* prevent overlap with photo */

    }

    #role {
      font-size: 1.25rem;
      margin-bottom: 0.5rem;
    }

    header h1 {
      margin: 0;
      /* font-size: 2rem; */
    }

    header address {
      font-style: normal;
      font-size: 0.9rem;
    }

    header p {
      margin: 0.5rem 0 0;
      font-weight: 500;
      color: #666;
    }

    main {
      column-count: 2;
      column-gap: 5rem;
      max-width: 1200px;
      margin: auto;
      padding: 1rem 1rem;
      color: #515151;
    }

    main > section:first-child > h2:first-child {
      margin-top: 0;
    }

    h2 {
      font-weight: 200;
      border-bottom: 2px solid #ddd;
      padding-bottom: 0.3rem;
      font-size: 1.75rem;
    }

    h3 {
      font-weight: 500;
      font-size: 1.25rem;
      /* margin-top: 0; */
    }

    .sub-role {
      break-inside: avoid;
      margin-bottom: 1.2rem;
    }

    .sub-role h4 {
      break-inside: avoid;
    }

    h4 {
      margin: 0.3rem 0 0.2rem;
      font-weight: 700;
      font-size: 1.05rem;
      /* background-color: #f0f0f0; */
    }

    small {
      font-weight: normal;
      color: #666;
      font-size: 0.85rem;
    }

    ul {
      margin-top: 0.4rem;
      margin-bottom: 1.5rem;
      padding-left: 1.2rem;
      break-inside: avoid;
    }

    table {
      border-collapse: collapse;
      width: 100%;
      margin-top: 1rem;
      /* font-size: 0.9rem; */
    }

    /*.past-experiences th,
    .past-experiences td {*/
    th, td {
      text-align: left;
      padding: 0.4rem 0.6rem;
      vertical-align: top;
    }

    .skill-group {
      font-weight: bold;
      width: 20%;
    }

    .skill-items {
      width: 80%;
    }

    #experiences {
    }

    .past-experiences {
      /* font-size: 0.9rem; */
    }

    .past-experience-title {
      break-inside: avoid;
    }

    .education-descr {
      margin-top: 0.4rem;
      margin-bottom: 1.5rem;
    }
    
    .lead {
      line-height: 1.6;
      font-size: 1.05rem;
      margin: auto;
      margin-bottom: 2rem;
      max-width: 1200px;
    }

    #lead-span {
        display: none;
    }

    #lead-flow {
        display: block;
    }

    .profile-photo {
        width: 90px;
        height: 90px;
        border-radius: 50%;
        object-fit: cover;
        margin-right: 1rem;
    }

    .logo {
        height: 1em;
        vertical-align: middle;
        margin-right: 0.5rem;
    }

    @media print {
      body {
        font-size: 11pt; /* smaller than screen size */
        line-height: 1.3; /* tighter but readable */
        -webkit-print-color-adjust: exact; /* preserve colors if needed */
      }

      main {
        column-count: 2;
        column-gap: 2.5rem; /* narrower gap for print */
      }

      @page {
        size: A4; /* or 'letter' depending on region */
        margin: 1.2cm; /* smaller margins for more space */
      }

      h1, h2, h3, h4 {
        page-break-after: avoid; /* prevent headings at bottom of column/page */
      }

      section, article, table {
        /* break-inside: avoid; */ /* avoid splitting sections across columns */
      }

      /* Optional: shrink tables for print */
      table {
        font-size: 10pt;
      }

      #lead-span {
          display: block;
      }

      #lead-flow {
          display: none;
      }
      .profile-photo {
          width: 70px;
          height: 70px;
      }
      .logo {
          height: 0.8em;
      }
    }
  </style>
</head>
<body>
  <header>
    <img src="https://www.bartkl.com/cv/assets/images/bart.png" alt="Profile photo" class="profile-photo">
    <div class="text-block">
      <h1>{{ header.name }}</h1>
      <div id="role">{{ header.role }}</div>
      <address>
          <i class="fa-solid fa-person"></i> he/him | <i class="fa-solid fa-house"></i> Lelystad, the Netherlands | <i class="fa-solid fa-envelope"></i> <a href="mailto:bartkl@gmail.com">bartkl@gmail.com</a> | <i class="fa-solid fa-globe"></i> <a href="https://www.bartkl.com" target="_blank" rel="noopener">bartkl.com</a> | <i class="fa-brands fa-linkedin"></i> <a href="https://www.linkedin.com/in/bartkl">bartkl</a>
      </address>
    </div>
  </header>
  <p id="lead-span" class="lead">
    {{ lead }}
  </p>
  <main>
    <p id="lead-flow" class="lead">
      {{ lead }}
    </p>

    <section id="experience">
      <h2>Work Experience</h2>

      <div id="experiences">

        <h3>Alliander</h3>
        <div class="sub-roles--alliander">
          {% for job in experience.alliander %}
          <div class="sub-role">
            <h4>{{ job.role }} ({{ job.context }})<br><small>{{ job.period }}</small></h4>
            <ul>
              {% for desc in job.description %}
              <li>{{ desc }}</li>
              {% endfor %}
            </ul>
          </div>
          {% endfor %}
        </div>

        <h3>Past Experiences</h3>
        <table class="past-experiences">
          <tbody>
            {% for job in experience.past %}
            <tr>
              <td class="past-experience-title"><strong>{{ job.role }} ({{ job.context}})</strong><br /><small>{{ job.period }}</small></td>
              <td class="past-experience-descr">{{ job.description }}</td>
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </section>

    <section id="skills"><!-- TODO: Move to fixed right column perhaps? Adjacent to Work experience (on the left) -->
      <h2>Skills & Competencies</h2>
      <table class="skills-table">
        <tbody>
          {% for skill in skills %}
          <tr>
            <td class="skill-group"><strong>{{ skill.group }}</strong></td>
            <td class="skill-items">
              {{ skill["items"] | join(', ') }}
              {#<ul style="column-count: 2; list-style: none">
              {% for item in skill["items"] %}
                <li>{{ item }}</li>
              {% endfor %}
              </ul>#}
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </section>


    <section id="education">
      <h2>Education</h2>
      <div class="education-items">
          {% for edu in education %}
          <div class="education-item">
            <h4>{{ edu.study }}<br /><small>{{ edu.period }}</small></h4>
            <p class="education-descr">{{ edu.description }}</p>
          </div>
          {% endfor %}
      </div>
    </section>

  </main>
</body>
</html>

